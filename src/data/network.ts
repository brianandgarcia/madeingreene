export interface NetworkMember {
  name: string;
  category: string;
  tagline: string;
  location: string;
  website?: string;
  phone?: string;
}

export const networkMembers: NetworkMember[] = [];
